const cashAvailable = [...cid];
  const registerStatus = {
   status: 'Open',
   change: cashAvailable
  }
  for(var i = cashAvailable.length - 1 ; i > 0; i--){
    for(var j = cashAvailable[i][1])
    switch(cashAvailable[i][0]){
      case 'ONE HUNDRED':
      if(cashAvailable[i][1] < 100){
        break
      } 
    }
  }